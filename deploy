#!/bin/bash

set -euo pipefail

if [[ ! -f .env ]]; then
  echo "deploy: .env is missing; refusing to continue" >&2
  exit 1
fi

git reset --hard && git fetch && git rebase
docker compose up -d --build --force-recreate
